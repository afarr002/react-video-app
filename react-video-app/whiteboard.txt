whiteboard ideas

(will update as project develops)

-- install dependencies
    -- packages you think you'll need for this project
        -- add vite congif file for server (vite will be the dev server)

-- create basic files and structure for app
    -- create main jsx file
    -- create app jsx file
    -- create app css filescreate index css file
    -- create dark theme in theme js file
    -- create GlobalStyle with import from styled-components
    -- create navbar component and style
    -- create sidebar component and style
    -- create mobilenavbar component and style

-- set up supabase (using instead of firebase to familiarize)
    -- set up authentication (google oauth? - supabase auth)
    -- auth navbar
    -- create profile based on auth sign in status
    -- auth sign oauth

-- upload videos
    -- uploading videos
    -- add video details

-- generic pages
    -- home page
    -- trending video page
    -- delete video page

-- video pages
    -- video watch page
    -- different video views?

-- subscriptions
    -- subscribe user
    -- view subscription page

-- user data
    -- users liked videos
    -- users watched video history

-- search
    -- search results page

-- user profile
    -- edit profile